/*const assert = require('assert');
const { getFileContents } = require('./utils');

describe('Your "sql_reference.json" file', () => {
  let jsonFile, copyOfJsonFile;
  before(async () => {
    jsonFile = await getFileContents('../sql_reference/sql_reference.json');
    copyOfJsonFile = await getFileContents('./test/sample_files/sql_reference.json');
  });

  it('should have the correct properties and values', async () => {
    assert(jsonFile === copyOfJsonFile);
  });
});*/
